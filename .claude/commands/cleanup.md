When this command is run, look through the repository and remove any test-files, any files that are no longer being used and anything that doesn't make sense anymore.

Go through the /docs folder, and update the documentation so that it fits the current state of the codebase.
Read and understand all the README.md files in the repos, and update them to be precisely as the relevant part of the code is showing.
Remove any tasks from tasks.md that has already been completed.